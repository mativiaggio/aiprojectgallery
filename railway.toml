[build]
builder = "RAILPACK"
buildCommand = "PLAYWRIGHT_BROWSERS_PATH=0 pnpm exec playwright install --with-deps chromium && PLAYWRIGHT_BROWSERS_PATH=0 pnpm build"
watchPatterns = [
  "src/**",
  "public/**",
  "drizzle/**",
  "scripts/**",
  "package.json",
  "pnpm-lock.yaml",
  "next.config.ts",
  "drizzle.config.ts",
  "tsconfig.json",
  "postcss.config.mjs",
  "components.json"
]

[deploy]
runtime = "V2"
numReplicas = 1
sleepApplication = true
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckTimeout = 60
preDeployCommand = "pnpm db:generate && pnpm db:migrate"
startCommand = "PLAYWRIGHT_BROWSERS_PATH=0 pnpm exec next start -H 0.0.0.0 -p $PORT"